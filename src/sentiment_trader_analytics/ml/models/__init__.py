"""Model definition modules for the machine learning pipeline.

Submodules define custom model builders, wrappers, or architecture
implementations used by ``training.py`` and ``evaluation.py``.

Current models are built via factories in ``training.py`` (RandomForest,
XGBoost, LightGBM). Custom model classes should be placed here.
"""
